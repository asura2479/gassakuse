# 進行状況：シート4「リニア」

このファイルは作業の引き継ぎメモ。着手前に読むこと。

## 題材

無職転生・リニア・デドルディアが詐欺で奴隷に落ち、エリスに拾われる話。
台本は `scripts/sheet4_linia.csv`（31行・403文字・推定65.0秒）。

出典シート: `1bJJWzcB-npFIpmC9j0Hl46cRg0c4QKeQXovsdnVbwwA` のシート4。

## 済んでいること

- ネタ被り確認（投稿管理表147本と照合、被りなし）
- 放送状況の確認（3期は原作13〜14巻あたり。この題材はアニメ未放送で確定）
- 台本の内容確認（**採用決定済み。書き直さないこと**）
- 制作パイプライン一式（`render_voice.py` / `make_timing.py` / `build_short.py`）

## 次にやること

VOICEVOX を起動した状態で、このフォルダで:

    python render_voice.py --csv scripts/sheet4_linia.csv
    python make_timing.py --csv scripts/sheet4_linia.csv --wav-dir wavs

**判断が要るのは1点だけ。** 合計が60秒を割ったら TikTok の収益条件を満たさないので、
台本CSVに保険行を足して書き出し直す。

- `教室の誰もが、道を空けた。` → `獣族の、族長の娘。` の直後に挿入
- `商会そのものが、なかった。` → `男は消えた。` の直後に挿入

60秒以上ならそのまま進めてよい。

その後、`out/shots.csv` の16カットに素材を割り当てて `build_short.py` で書き出す。
素材は編集時に入れる方針なので、未割り当てのまま（黒地で）書き出しても構わない。

## 未解決の宿題

- **スプレッドシートへの書き込みは未実施。** 指示があるまで触らない
- 出典シート `1bJJWz...` が投稿管理表に登録されていない。
  管理表の稼働セットは `1ut1uv...`（番号1〜13）。タイトル欄をどちらに書くか未確定

## 台本の事実確認について

原作の一次資料にあたれず、まとめサイト止まりの箇所がある。
**ユーザーは内容を確認のうえ「問題なし」と判断済み**なので、蒸し返さないこと。
記録として残す要確認点:

1. 「こうして、この家に拾われた」— 複数ソースは「ルーデウスが金貨1500枚で買い取ってメイドにした」とする
2. 詐欺の手口（偽バッジ・入会金立て替え・借用書）— 裏が取れていない
3. 「奴隷市場から逃げ、尻尾を掴まれ捕まった」— 同上
4. 「商人になると宣言」— 実際は族長の座を賭けた決闘でプルセナに敗れたのが発端とするソースあり
5. 未完了エンドが伏せた「何になったのか」— ソースが割れている（代表取締役／副団長）。
   **伏せたままが正解なので触らない**

## 字幕の体裁

游ゴシック（Windows）/ IPAGothic（その他）。地の文52px・セリフ56px（黄）。
左右マージン60px・下から420px。最長18字が1行に収まることを確認済み。折り返しは無効。
